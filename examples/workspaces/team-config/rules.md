# Team-Specific Rules

This file comes from a different source directory (team-config).

## Code Review Requirements

- All PRs require at least one approval
- CI must pass before merge
- Use squash merge for clean history
